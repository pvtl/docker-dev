<VirtualHost *:80>
    ServerName my.pub.localhost
    ServerAlias *.pub.localhost

    SetOutputFilter DEFLATE

    # NOTE: this config will map "firefly.pub.localhost" to /var/www/html/firefly/public
    UseCanonicalName Off
    VirtualDocumentRoot /var/www/html/%1/public/

    <Directory /var/www/html>
        Options +FollowSymLinks -Indexes -Includes

        AllowOverride All
        Order deny,allow
        Allow from all
        Require all granted
    </Directory>
</VirtualHost>

<VirtualHost *:80>
    ServerName my.php5.localhost
    ServerAlias *.php5.localhost

    # NOTE: this config will map "firefly.php5.localhost" to /var/www/html/firefly/public (and use PHP5)
    ProxyPreserveHost On
    ProxyPass / http://pvtl-php5/
    ProxyPassReverse / http://pvtl-php5/
</VirtualHost>

<VirtualHost *:80>
    ServerName my.localhost
    ServerAlias *.localhost

    SetOutputFilter DEFLATE

    # NOTE: this config will map "firefly.localhost" to /var/www/html/firefly
    UseCanonicalName Off
    VirtualDocumentRoot /var/www/html/%1/

    <Directory /var/www/html>
        Options +FollowSymLinks -Indexes -Includes

        AllowOverride All
        Order deny,allow
        Allow from all
        Require all granted
    </Directory>
</VirtualHost>
